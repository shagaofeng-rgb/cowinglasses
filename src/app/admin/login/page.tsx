import Link from "next/link";
import { AdminLoginForm } from "@/app/admin/login/login-form";
import { isDatabaseConfigured } from "@/db/client";

export default function AdminLoginPage() {
  const configured = isDatabaseConfigured();
  return <main className="grid min-h-screen bg-[#f4f7f4] lg:grid-cols-[1.1fr_0.9fr]"><section className="hidden bg-[#111b15] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div><p className="text-2xl font-black tracking-[-0.05em]">COWIN<span className="text-[#b8e629]">.</span></p><p className="mt-2 text-sm uppercase tracking-[0.16em] text-white/55">Retail operations</p></div><div><p className="text-sm font-bold uppercase tracking-[0.16em] text-[#b8e629]">COWIN Glasses 后台</p><h1 className="mt-4 max-w-lg font-serif text-6xl font-bold leading-[0.96] tracking-[-0.06em]">运营每一笔真实业务。</h1><p className="mt-6 max-w-md leading-7 text-white/64">商品、库存、订单、支付、物流与客户运营集中在一个经过权限控制的工作台。</p></div><p className="text-sm text-white/45">仅限已授权管理员访问</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md rounded-3xl border border-black/10 bg-white p-7 shadow-[0_24px_60px_rgba(10,30,17,0.1)] sm:p-9"><Link href="/en" className="text-sm font-bold text-[#4e7f3f]">← 返回商城</Link><p className="mt-10 text-xs font-bold uppercase tracking-[0.16em] text-[#538a42]">管理员登录</p><h2 className="mt-3 font-serif text-4xl font-bold tracking-[-0.04em]">欢迎回来</h2><p className="mt-3 text-sm leading-6 text-black/58">{configured ? "使用已授权的 COWIN Glasses 管理员账号登录。" : "请先配置 Neon PostgreSQL 并执行管理员种子初始化；在此之前不允许进入后台。"}</p><AdminLoginForm configured={configured} /><p className="mt-5 text-center text-xs leading-5 text-black/45">初始超级管理员将通过服务端环境变量和一次性种子流程创建，不会写入前端代码。</p></div></section></main>;
}
