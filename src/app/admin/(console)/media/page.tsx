import { desc } from "drizzle-orm";
import { MediaRegisterForm } from "@/components/admin/content-forms";
import { getDatabase } from "@/db/client";
import { mediaAssets } from "@/db/schema";
import { requirePermission } from "@/lib/admin/auth";
export default async function MediaPage() { await requirePermission("customers.read"); const rows = await getDatabase().select().from(mediaAssets).orderBy(desc(mediaAssets.createdAt)).limit(100); return <main className="mx-auto max-w-[1560px] px-5 py-7 sm:px-8 lg:px-10"><header className="border-b border-black/10 pb-7"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#538a42]">客户与内容</p><h1 className="mt-2 font-serif text-4xl font-bold tracking-[-.04em]">媒体库</h1><p className="mt-2 text-sm text-black/55">当前可登记并复用已托管的媒体 URL。对象存储凭据接入前，系统不会把文件上传到未知位置。</p></header><section className="mt-6 grid gap-6 xl:grid-cols-[390px_minmax(0,1fr)]"><aside><h2 className="mb-3 font-serif text-2xl font-bold">登记外部媒体</h2><MediaRegisterForm/><p className="mt-3 rounded-xl bg-[#f5f7f4] p-4 text-xs leading-5 text-black/55">待提供对象存储的 Bucket、区域、上传签名和访问域名后，此处将启用真实文件上传与图片压缩。</p></aside><div className="overflow-hidden rounded-2xl border border-black/10 bg-white"><table className="w-full text-left text-sm"><thead className="bg-[#f5f7f4] text-xs uppercase text-black/50"><tr><th className="p-4">资源</th><th className="p-4">类型</th><th className="p-4">替代文本</th><th className="p-4">登记时间</th></tr></thead><tbody>{rows.length ? rows.map((row) => <tr key={row.id} className="border-t border-black/8"><td className="max-w-[360px] truncate p-4"><a href={row.url} target="_blank" rel="noreferrer" className="font-medium text-[#477b38] underline underline-offset-4">{row.url}</a></td><td className="p-4">{row.mimeType || "—"}</td><td className="p-4">{row.altText || "—"}</td><td className="p-4 text-black/55">{row.createdAt.toLocaleString("zh-CN")}</td></tr>) : <tr><td colSpan={4} className="p-12 text-center text-black/55">暂无已登记媒体。</td></tr>}</tbody></table></div></section></main>; }
