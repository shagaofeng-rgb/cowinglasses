import { asc } from "drizzle-orm"; import { CustomerTagForm } from "@/components/admin/customer-operation-forms"; import { getDatabase } from "@/db/client"; import { customerTags } from "@/db/schema"; import { requirePermission } from "@/lib/admin/auth";
export default async function CustomerTagsPage(){await requirePermission("customers.read");const rows=await getDatabase().select().from(customerTags).orderBy(asc(customerTags.name));return <main className="mx-auto max-w-[1560px] px-5 py-7 sm:px-8 lg:px-10"><header className="border-b border-black/10 pb-7"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#538a42]">客户与内容</p><h1 className="mt-2 font-serif text-4xl font-bold">客户标签与分组</h1><p className="mt-2 text-sm text-black/55">管理客户运营标签。客户标签分配将在客户详情编辑器中继续接入。</p></header><section className="mt-6 grid gap-6 xl:grid-cols-[360px_minmax(0,1fr)]"><aside><CustomerTagForm/></aside><div className="grid gap-3">{rows.map(row=><details key={row.id} className="rounded-xl border border-black/10 bg-white"><summary className="flex cursor-pointer list-none items-center gap-3 p-4"><i className="h-3 w-3 rounded-full" style={{background:row.color||"#548544"}}/><strong>{row.name}</strong></summary><div className="border-t border-black/8 p-4"><CustomerTagForm record={row}/></div></details>)}</div></section></main>}
