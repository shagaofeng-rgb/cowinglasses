import { asc } from "drizzle-orm"; import { MembershipTierForm } from "@/components/admin/customer-operation-forms"; import { getDatabase } from "@/db/client"; import { membershipTiers } from "@/db/schema"; import { requirePermission } from "@/lib/admin/auth";
export default async function MembershipsPage(){await requirePermission("customers.read");const rows=await getDatabase().select().from(membershipTiers).orderBy(asc(membershipTiers.thresholdAmount));return <main className="mx-auto max-w-[1560px] px-5 py-7 sm:px-8 lg:px-10"><header className="border-b border-black/10 pb-7"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#538a42]">客户与内容</p><h1 className="mt-2 font-serif text-4xl font-bold">会员等级</h1><p className="mt-2 text-sm text-black/55">设置累计消费门槛与可扩展会员权益。等级自动归属规则将在真实订单累计数据接入后执行。</p></header><section className="mt-6 grid gap-6 xl:grid-cols-[360px_minmax(0,1fr)]"><aside><MembershipTierForm/></aside><div className="grid gap-3">{rows.map(row=><details key={row.id} className="rounded-xl border border-black/10 bg-white"><summary className="flex cursor-pointer list-none justify-between p-4"><strong>{row.name}</strong><span>USD {row.thresholdAmount}</span></summary><div className="border-t border-black/8 p-4"><MembershipTierForm record={row}/></div></details>)}</div></section></main>}
