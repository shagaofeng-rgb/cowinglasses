import { and, gte, lte } from "drizzle-orm";
import { getDatabase } from "@/db/client";
import { storefrontEvents } from "@/db/schema";
import { getDateRange } from "@/lib/admin/date-range";
import { requirePermission } from "@/lib/admin/auth";
import { DateRangeFilter } from "@/components/admin/date-range-filter";

export default async function FunnelPage({ searchParams }: { searchParams: Promise<{ range?: string; from?: string; to?: string }> }) { await requirePermission("analytics.read"); const range = getDateRange(await searchParams); const rows = await getDatabase().select({ name: storefrontEvents.eventName }).from(storefrontEvents).where(and(gte(storefrontEvents.createdAt, range.from), lte(storefrontEvents.createdAt, range.to))).limit(10000); const steps = [["浏览页面", "page_view"], ["浏览商品", "product_view"], ["加入购物车", "add_to_cart"], ["开始结算", "begin_checkout"], ["创建订单", "order_created"]] as const; const base = rows.filter((row) => row.name === "page_view").length || 1; return <main className="mx-auto max-w-5xl px-5 py-7 sm:px-8 lg:px-10"><h1 className="font-serif text-4xl font-bold">转化漏斗</h1><p className="mt-2 text-sm text-black/55">{range.from.toLocaleDateString("zh-CN")} 至 {range.to.toLocaleDateString("zh-CN")}，按照前台真实事件统计。</p><form className="mt-5 rounded-2xl border border-black/10 bg-white p-4"><DateRangeFilter range={range}/><button className="mt-3 rounded-xl bg-[#17231c] px-4 py-2.5 text-sm font-bold text-white">筛选漏斗</button></form><div className="mt-6 grid gap-4">{steps.map(([label, name]) => { const count = rows.filter((row) => row.name === name).length; return <div key={name} className="rounded-2xl border border-black/10 bg-white p-5"><div className="flex justify-between"><strong>{label}</strong><strong>{count}</strong></div><div className="mt-3 h-3 rounded-full bg-[#edf0eb]"><div className="h-3 rounded-full bg-[#a6c947]" style={{ width: `${Math.min(100, count / base * 100)}%` }}/></div><p className="mt-2 text-xs text-black/50">相对页面浏览 {((count / base) * 100).toFixed(2)}%</p></div>; })}</div></main>; }
