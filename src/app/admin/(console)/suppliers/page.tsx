import { desc } from "drizzle-orm";
import { SupplierForm } from "@/components/admin/catalog-forms";
import { getDatabase } from "@/db/client";
import { suppliers } from "@/db/schema";
import { requirePermission } from "@/lib/admin/auth";

export default async function SuppliersPage() { await requirePermission("catalog.read"); const rows = await getDatabase().select().from(suppliers).orderBy(desc(suppliers.createdAt)); return <div className="mx-auto max-w-[1560px] px-5 py-7 sm:px-8 lg:px-10"><header className="border-b border-black/10 pb-7"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#538a42]">商品</p><h1 className="mt-2 font-serif text-4xl font-bold tracking-[-.04em]">供应商管理</h1><p className="mt-2 text-sm text-black/55">维护供应商与采购对接资料。供应商数据仅在后台和服务端使用。</p></header><section className="mt-6 grid gap-6 xl:grid-cols-[390px_minmax(0,1fr)]"><aside><h2 className="mb-3 font-serif text-2xl font-bold">新建供应商</h2><SupplierForm/></aside><div className="grid gap-4">{rows.length ? rows.map((row) => <details key={row.id} className="group rounded-2xl border border-black/10 bg-white"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 p-5"><div><p className="font-bold">{row.name}</p><p className="mt-1 text-xs text-black/50">{row.contactName || "未填写联系人"} · {row.email || row.phone || "暂无联系方式"}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${row.isActive ? "bg-[#edf4e9] text-[#3e752e]" : "bg-black/5 text-black/50"}`}>{row.isActive ? "启用" : "停用"}</span></summary><div className="border-t border-black/8 p-5"><SupplierForm record={row}/></div></details>) : <div className="rounded-2xl border border-dashed border-black/15 bg-white p-12 text-center text-sm text-black/55">暂无供应商，请先创建一条供应商资料。</div>}</div></section></div>; }
