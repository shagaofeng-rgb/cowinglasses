import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Locale } from "@/lib/i18n";

export function Footer({ locale }: { locale: Locale }) { return <footer className="border-t border-[var(--line)] bg-[var(--dark)] text-white"><div className="shell grid gap-10 py-14 md:grid-cols-[1.4fr_1fr_1fr]"><div><p className="text-2xl font-black tracking-[-.08em]">COWIN<span className="text-[var(--lime)]">.</span></p><p className="mt-4 max-w-xs text-sm leading-6 text-zinc-300">English product catalogue for CoWin smart glasses.</p></div><FooterGroup title="Products" links={[["Browse products", "/shop"], ["Explore", "/collections/explore"], ["Everyday", "/collections/everyday"]]} locale={locale}/><FooterGroup title="Product information" links={[["Before placing an order", "/support/contact"], ["Music & movement", "/collections/music-movement"]]} locale={locale}/></div><div className="shell border-t border-white/15 py-5 text-xs text-zinc-400">© 2026 CoWin Glasses. English product catalogue.</div></footer>; }
function FooterGroup({ title, links, locale }: { title: string; links: [string, string][]; locale: Locale }) { return <div><h2 className="text-sm font-bold">{title}</h2><ul className="mt-4 grid gap-3">{links.map(([label, href]) => <li key={href}><Link className="inline-flex items-center gap-1 text-sm text-zinc-300 hover:text-[var(--lime)]" href={`/${locale}${href}`}>{label}<ArrowUpRight size={13}/></Link></li>)}</ul></div>; }
