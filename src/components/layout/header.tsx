"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
import type { Locale } from "@/lib/i18n";

const links = [["Products", "/shop"], ["Explore", "/collections/explore"], ["Product information", "/support/contact"]] as const;
export function Header({ locale }: { locale: Locale }) { const [open, setOpen] = useState(false); return <><div className="bg-black px-5 py-3 text-center text-xs font-medium text-white">English product catalogue · Pricing and availability on request</div><header className="sticky top-0 z-30 border-b border-[var(--line)] bg-[color:rgba(248,248,245,.94)] backdrop-blur"><div className="shell flex h-[72px] items-center justify-between gap-4"><Link href={`/${locale}`} className="text-2xl font-black tracking-[-.1em]" aria-label="CoWin Glasses home">COWIN<span className="text-[#94aa10]">.</span></Link><nav className="hidden items-center gap-8 lg:flex" aria-label="Main navigation">{links.map(([label, href]) => <Link className="text-sm font-semibold hover:text-[#6b7e0d]" key={href} href={`/${locale}${href}`}>{label}</Link>)}</nav><Link className="button-primary hidden lg:inline-flex" href={`/${locale}/shop`}>Browse products</Link><button className="lg:hidden" aria-label={open ? "Close menu" : "Open menu"} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-[var(--line)] bg-[var(--paper)] lg:hidden"><nav className="shell grid gap-1 py-4" aria-label="Mobile navigation">{links.map(([label, href]) => <Link onClick={() => setOpen(false)} className="rounded-xl px-3 py-3 font-bold hover:bg-white" key={href} href={`/${locale}${href}`}>{label}</Link>)}</nav></div>}</header></>; }
