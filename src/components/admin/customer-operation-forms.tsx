"use client";
import { useActionState } from "react";
import { initialCustomerOperationState, saveTagAction, saveTierAction } from "@/app/admin/(console)/customer-operations/actions";
const input="mt-1 w-full rounded-xl border border-black/12 bg-white px-3 py-2.5 text-sm outline-none focus:border-[#548544]"; function Notice({message,success}:{message:string;success:boolean}){return message?<p role="status" className={`rounded-xl p-3 text-sm ${success?"bg-[#eef8e8] text-[#376c2a]":"bg-red-50 text-red-700"}`}>{message}</p>:null;}
export function CustomerTagForm({record}:{record?:{id:string;name:string;color:string|null}}){const[state,action,pending]=useActionState(saveTagAction,initialCustomerOperationState);return <form action={action} className="grid gap-3 rounded-2xl border border-black/10 bg-white p-5"><input type="hidden" name="id" value={record?.id??""}/><label className="text-sm font-semibold">标签名称<input required name="name" defaultValue={record?.name} className={input}/></label><label className="text-sm font-semibold">标识色<input name="color" defaultValue={record?.color??""} placeholder="#548544" className={input}/></label><Notice {...state}/><button disabled={pending} className="rounded-xl bg-[#17231c] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{pending?"正在保存…":"保存标签"}</button></form>}
export function MembershipTierForm({record}:{record?:{id:string;name:string;thresholdAmount:string;benefits:Record<string,unknown>}}){const[state,action,pending]=useActionState(saveTierAction,initialCustomerOperationState);return <form action={action} className="grid gap-3 rounded-2xl border border-black/10 bg-white p-5"><input type="hidden" name="id" value={record?.id??""}/><label className="text-sm font-semibold">等级名称<input required name="name" defaultValue={record?.name} className={input}/></label><label className="text-sm font-semibold">累计消费门槛（USD）<input required name="thresholdAmount" defaultValue={record?.thresholdAmount??"0"} className={input}/></label><label className="text-sm font-semibold">权益 JSON<textarea name="benefits" rows={4} defaultValue={record?JSON.stringify(record.benefits):"{}"} className={`${input} font-mono text-xs`}/></label><Notice {...state}/><button disabled={pending} className="rounded-xl bg-[#17231c] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{pending?"正在保存…":"保存等级"}</button></form>}
